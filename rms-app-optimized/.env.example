# TCG RMS — copy to .env (gitignored). Commit keys only, never values.
DATABASE_URL=
PG_SCHEMA=
MINIO_ENDPOINT=
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MINIO_SECURE=false
MINIO_BUCKET=
ANTHROPIC_API_KEY=
JWT_SECRET=
JWT_EXPIRE_MINUTES=480
CORS_ORIGINS=http://localhost:3000
APP_ENV=dev
MAX_UPLOAD_MB=10
AGENT_TIMEOUT_S=60
