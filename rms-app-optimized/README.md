# rms-app — TCG Digital Recruitment Management System

Implementation workspace. See [architecture.md](architecture.md) for the HLA and
`../RMS_LLD.md` for the authoritative design. Build order: `../plan.md`.

Layout: `backend/` (Python 3.11 + FastAPI), `frontend/` (React 19 + Vite + TS),
`docker-compose.yml`, `.env.example`.
